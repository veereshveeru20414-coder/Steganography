# Steganography
The Steganography Project is a C-based application that hides and extracts secret data inside BMP images using the LSB technique. It supports encoding and decoding through command-line arguments and can securely embed text, C files, shell scripts, or header files without visible image changes.
